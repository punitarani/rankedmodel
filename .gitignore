# dependencies
node_modules/

# build outputs
dist/
.tanstack/
*.tsbuildinfo

# cloudflare local state
.wrangler/
.dev.vars*

# env
.env
.env.*

# test artifacts
test-results/
playwright-report/
coverage/

# design handoff archive (extracted copy lives in docs/design-handoff/)
*.zip

# local session tooling
.remember/

# private strategy notes — never commit (history was scrubbed of this file)
COMPETITIVE_ANALYSIS.md

# os
.DS_Store

# user-local Claude Code state (launch.json is shared dev config and is tracked)
.claude/settings.local.json
